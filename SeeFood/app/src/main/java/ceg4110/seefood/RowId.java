package ceg4110.seefood;


/**
 * Returns an array of row id's
 */
public class RowId {

//    public RowId(){}

    public int[] getRowIdArray(int numberOfRows) {

        int[] rowIds = new int[numberOfRows+20];

        int i = 0;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row1;

        rowIds[i++] = R.id.row2;

        rowIds[i++] = R.id.row3;

        rowIds[i++] = R.id.row4;

        rowIds[i++] = R.id.row5;

        rowIds[i++] = R.id.row6;

        rowIds[i++] = R.id.row7;

        rowIds[i++] = R.id.row8;

        rowIds[i++] = R.id.row9;

        rowIds[i++] = R.id.row10;

        rowIds[i++] = R.id.row11;

        rowIds[i++] = R.id.row12;

        rowIds[i++] = R.id.row13;

        rowIds[i++] = R.id.row14;

        rowIds[i++] = R.id.row15;

        rowIds[i++] = R.id.row16;

        rowIds[i++] = R.id.row17;

        rowIds[i++] = R.id.row18;

        rowIds[i++] = R.id.row19;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row20;

        rowIds[i++] = R.id.row21;

        rowIds[i++] = R.id.row22;

        rowIds[i++] = R.id.row23;

        rowIds[i++] = R.id.row24;

        rowIds[i++] = R.id.row25;

        rowIds[i++] = R.id.row26;

        rowIds[i++] = R.id.row27;

        rowIds[i++] = R.id.row28;

        rowIds[i++] = R.id.row29;

        rowIds[i++] = R.id.row30;

        rowIds[i++] = R.id.row31;

        rowIds[i++] = R.id.row32;

        rowIds[i++] = R.id.row33;

        rowIds[i++] = R.id.row34;

        rowIds[i++] = R.id.row35;

        rowIds[i++] = R.id.row36;

        rowIds[i++] = R.id.row37;

        rowIds[i++] = R.id.row38;

        rowIds[i++] = R.id.row39;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row40;

        rowIds[i++] = R.id.row41;

        rowIds[i++] = R.id.row42;

        rowIds[i++] = R.id.row43;

        rowIds[i++] = R.id.row44;

        rowIds[i++] = R.id.row45;

        rowIds[i++] = R.id.row46;

        rowIds[i++] = R.id.row47;

        rowIds[i++] = R.id.row48;

        rowIds[i++] = R.id.row49;

        rowIds[i++] = R.id.row50;

        rowIds[i++] = R.id.row51;

        rowIds[i++] = R.id.row52;

        rowIds[i++] = R.id.row53;

        rowIds[i++] = R.id.row54;

        rowIds[i++] = R.id.row55;

        rowIds[i++] = R.id.row56;

        rowIds[i++] = R.id.row57;

        rowIds[i++] = R.id.row58;

        rowIds[i++] = R.id.row59;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row60;

        rowIds[i++] = R.id.row61;

        rowIds[i++] = R.id.row62;

        rowIds[i++] = R.id.row63;

        rowIds[i++] = R.id.row64;

        rowIds[i++] = R.id.row65;

        rowIds[i++] = R.id.row66;

        rowIds[i++] = R.id.row67;

        rowIds[i++] = R.id.row68;

        rowIds[i++] = R.id.row69;

        rowIds[i++] = R.id.row70;

        rowIds[i++] = R.id.row71;

        rowIds[i++] = R.id.row72;

        rowIds[i++] = R.id.row73;

        rowIds[i++] = R.id.row74;

        rowIds[i++] = R.id.row75;

        rowIds[i++] = R.id.row76;

        rowIds[i++] = R.id.row77;

        rowIds[i++] = R.id.row78;

        rowIds[i++] = R.id.row79;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row80;

        rowIds[i++] = R.id.row81;

        rowIds[i++] = R.id.row82;

        rowIds[i++] = R.id.row83;

        rowIds[i++] = R.id.row84;

        rowIds[i++] = R.id.row85;

        rowIds[i++] = R.id.row86;

        rowIds[i++] = R.id.row87;

        rowIds[i++] = R.id.row88;

        rowIds[i++] = R.id.row89;

        rowIds[i++] = R.id.row90;

        rowIds[i++] = R.id.row91;

        rowIds[i++] = R.id.row92;

        rowIds[i++] = R.id.row93;

        rowIds[i++] = R.id.row94;

        rowIds[i++] = R.id.row95;

        rowIds[i++] = R.id.row96;

        rowIds[i++] = R.id.row97;

        rowIds[i++] = R.id.row98;

        rowIds[i++] = R.id.row99;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row100;

        rowIds[i++] = R.id.row101;

        rowIds[i++] = R.id.row102;

        rowIds[i++] = R.id.row103;

        rowIds[i++] = R.id.row104;

        rowIds[i++] = R.id.row105;

        rowIds[i++] = R.id.row106;

        rowIds[i++] = R.id.row107;

        rowIds[i++] = R.id.row108;

        rowIds[i++] = R.id.row109;

        rowIds[i++] = R.id.row110;

        rowIds[i++] = R.id.row111;

        rowIds[i++] = R.id.row112;

        rowIds[i++] = R.id.row113;

        rowIds[i++] = R.id.row114;

        rowIds[i++] = R.id.row115;

        rowIds[i++] = R.id.row116;

        rowIds[i++] = R.id.row117;

        rowIds[i++] = R.id.row118;

        rowIds[i++] = R.id.row119;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row120;

        rowIds[i++] = R.id.row121;

        rowIds[i++] = R.id.row122;

        rowIds[i++] = R.id.row123;

        rowIds[i++] = R.id.row124;

        rowIds[i++] = R.id.row125;

        rowIds[i++] = R.id.row126;

        rowIds[i++] = R.id.row127;

        rowIds[i++] = R.id.row128;

        rowIds[i++] = R.id.row129;

        rowIds[i++] = R.id.row130;

        rowIds[i++] = R.id.row131;

        rowIds[i++] = R.id.row132;

        rowIds[i++] = R.id.row133;

        rowIds[i++] = R.id.row134;

        rowIds[i++] = R.id.row135;

        rowIds[i++] = R.id.row136;

        rowIds[i++] = R.id.row137;

        rowIds[i++] = R.id.row138;

        rowIds[i++] = R.id.row139;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row140;

        rowIds[i++] = R.id.row141;

        rowIds[i++] = R.id.row142;

        rowIds[i++] = R.id.row143;

        rowIds[i++] = R.id.row144;

        rowIds[i++] = R.id.row145;

        rowIds[i++] = R.id.row146;

        rowIds[i++] = R.id.row147;

        rowIds[i++] = R.id.row148;

        rowIds[i++] = R.id.row149;

        rowIds[i++] = R.id.row150;

        rowIds[i++] = R.id.row151;

        rowIds[i++] = R.id.row152;

        rowIds[i++] = R.id.row153;

        rowIds[i++] = R.id.row154;

        rowIds[i++] = R.id.row155;

        rowIds[i++] = R.id.row156;

        rowIds[i++] = R.id.row157;

        rowIds[i++] = R.id.row158;

        rowIds[i++] = R.id.row159;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row160;

        rowIds[i++] = R.id.row161;

        rowIds[i++] = R.id.row162;

        rowIds[i++] = R.id.row163;

        rowIds[i++] = R.id.row164;

        rowIds[i++] = R.id.row165;

        rowIds[i++] = R.id.row166;

        rowIds[i++] = R.id.row167;

        rowIds[i++] = R.id.row168;

        rowIds[i++] = R.id.row169;

        rowIds[i++] = R.id.row170;

        rowIds[i++] = R.id.row171;

        rowIds[i++] = R.id.row172;

        rowIds[i++] = R.id.row173;

        rowIds[i++] = R.id.row174;

        rowIds[i++] = R.id.row175;

        rowIds[i++] = R.id.row176;

        rowIds[i++] = R.id.row177;

        rowIds[i++] = R.id.row178;

        rowIds[i++] = R.id.row179;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row180;

        rowIds[i++] = R.id.row181;

        rowIds[i++] = R.id.row182;

        rowIds[i++] = R.id.row183;

        rowIds[i++] = R.id.row184;

        rowIds[i++] = R.id.row185;

        rowIds[i++] = R.id.row186;

        rowIds[i++] = R.id.row187;

        rowIds[i++] = R.id.row188;

        rowIds[i++] = R.id.row189;

        rowIds[i++] = R.id.row190;

        rowIds[i++] = R.id.row191;

        rowIds[i++] = R.id.row192;

        rowIds[i++] = R.id.row193;

        rowIds[i++] = R.id.row194;

        rowIds[i++] = R.id.row195;

        rowIds[i++] = R.id.row196;

        rowIds[i++] = R.id.row197;

        rowIds[i++] = R.id.row198;

        rowIds[i++] = R.id.row199;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row200;

        rowIds[i++] = R.id.row201;

        rowIds[i++] = R.id.row202;

        rowIds[i++] = R.id.row203;

        rowIds[i++] = R.id.row204;

        rowIds[i++] = R.id.row205;

        rowIds[i++] = R.id.row206;

        rowIds[i++] = R.id.row207;

        rowIds[i++] = R.id.row208;

        rowIds[i++] = R.id.row209;

        rowIds[i++] = R.id.row210;

        rowIds[i++] = R.id.row211;

        rowIds[i++] = R.id.row212;

        rowIds[i++] = R.id.row213;

        rowIds[i++] = R.id.row214;

        rowIds[i++] = R.id.row215;

        rowIds[i++] = R.id.row216;

        rowIds[i++] = R.id.row217;

        rowIds[i++] = R.id.row218;

        rowIds[i++] = R.id.row219;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row220;

        rowIds[i++] = R.id.row221;

        rowIds[i++] = R.id.row222;

        rowIds[i++] = R.id.row223;

        rowIds[i++] = R.id.row224;

        rowIds[i++] = R.id.row225;

        rowIds[i++] = R.id.row226;

        rowIds[i++] = R.id.row227;

        rowIds[i++] = R.id.row228;

        rowIds[i++] = R.id.row229;

        rowIds[i++] = R.id.row230;

        rowIds[i++] = R.id.row231;

        rowIds[i++] = R.id.row232;

        rowIds[i++] = R.id.row233;

        rowIds[i++] = R.id.row234;

        rowIds[i++] = R.id.row235;

        rowIds[i++] = R.id.row236;

        rowIds[i++] = R.id.row237;

        rowIds[i++] = R.id.row238;

        rowIds[i++] = R.id.row239;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row240;

        rowIds[i++] = R.id.row241;

        rowIds[i++] = R.id.row242;

        rowIds[i++] = R.id.row243;

        rowIds[i++] = R.id.row244;

        rowIds[i++] = R.id.row245;

        rowIds[i++] = R.id.row246;

        rowIds[i++] = R.id.row247;

        rowIds[i++] = R.id.row248;

        rowIds[i++] = R.id.row249;

        rowIds[i++] = R.id.row250;

        rowIds[i++] = R.id.row251;

        rowIds[i++] = R.id.row252;

        rowIds[i++] = R.id.row253;

        rowIds[i++] = R.id.row254;

        rowIds[i++] = R.id.row255;

        rowIds[i++] = R.id.row256;

        rowIds[i++] = R.id.row257;

        rowIds[i++] = R.id.row258;

        rowIds[i++] = R.id.row259;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row260;

        rowIds[i++] = R.id.row261;

        rowIds[i++] = R.id.row262;

        rowIds[i++] = R.id.row263;

        rowIds[i++] = R.id.row264;

        rowIds[i++] = R.id.row265;

        rowIds[i++] = R.id.row266;

        rowIds[i++] = R.id.row267;

        rowIds[i++] = R.id.row268;

        rowIds[i++] = R.id.row269;

        rowIds[i++] = R.id.row270;

        rowIds[i++] = R.id.row271;

        rowIds[i++] = R.id.row272;

        rowIds[i++] = R.id.row273;

        rowIds[i++] = R.id.row274;

        rowIds[i++] = R.id.row275;

        rowIds[i++] = R.id.row276;

        rowIds[i++] = R.id.row277;

        rowIds[i++] = R.id.row278;

        rowIds[i++] = R.id.row279;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row280;

        rowIds[i++] = R.id.row281;

        rowIds[i++] = R.id.row282;

        rowIds[i++] = R.id.row283;

        rowIds[i++] = R.id.row284;

        rowIds[i++] = R.id.row285;

        rowIds[i++] = R.id.row286;

        rowIds[i++] = R.id.row287;

        rowIds[i++] = R.id.row288;

        rowIds[i++] = R.id.row289;

        rowIds[i++] = R.id.row290;

        rowIds[i++] = R.id.row291;

        rowIds[i++] = R.id.row292;

        rowIds[i++] = R.id.row293;

        rowIds[i++] = R.id.row294;

        rowIds[i++] = R.id.row295;

        rowIds[i++] = R.id.row296;

        rowIds[i++] = R.id.row297;

        rowIds[i++] = R.id.row298;

        rowIds[i++] = R.id.row299;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row300;

        rowIds[i++] = R.id.row301;

        rowIds[i++] = R.id.row302;

        rowIds[i++] = R.id.row303;

        rowIds[i++] = R.id.row304;

        rowIds[i++] = R.id.row305;

        rowIds[i++] = R.id.row306;

        rowIds[i++] = R.id.row307;

        rowIds[i++] = R.id.row308;

        rowIds[i++] = R.id.row309;

        rowIds[i++] = R.id.row310;

        rowIds[i++] = R.id.row311;

        rowIds[i++] = R.id.row312;

        rowIds[i++] = R.id.row313;

        rowIds[i++] = R.id.row314;

        rowIds[i++] = R.id.row315;

        rowIds[i++] = R.id.row316;

        rowIds[i++] = R.id.row317;

        rowIds[i++] = R.id.row318;

        rowIds[i++] = R.id.row319;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row320;

        rowIds[i++] = R.id.row321;

        rowIds[i++] = R.id.row322;

        rowIds[i++] = R.id.row323;

        rowIds[i++] = R.id.row324;

        rowIds[i++] = R.id.row325;

        rowIds[i++] = R.id.row326;

        rowIds[i++] = R.id.row327;

        rowIds[i++] = R.id.row328;

        rowIds[i++] = R.id.row329;

        rowIds[i++] = R.id.row330;

        rowIds[i++] = R.id.row331;

        rowIds[i++] = R.id.row332;

        rowIds[i++] = R.id.row333;

        rowIds[i++] = R.id.row334;

        rowIds[i++] = R.id.row335;

        rowIds[i++] = R.id.row336;

        rowIds[i++] = R.id.row337;

        rowIds[i++] = R.id.row338;

        rowIds[i++] = R.id.row339;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row340;

        rowIds[i++] = R.id.row341;

        rowIds[i++] = R.id.row342;

        rowIds[i++] = R.id.row343;

        rowIds[i++] = R.id.row344;

        rowIds[i++] = R.id.row345;

        rowIds[i++] = R.id.row346;

        rowIds[i++] = R.id.row347;

        rowIds[i++] = R.id.row348;

        rowIds[i++] = R.id.row349;

        rowIds[i++] = R.id.row350;

        rowIds[i++] = R.id.row351;

        rowIds[i++] = R.id.row352;

        rowIds[i++] = R.id.row353;

        rowIds[i++] = R.id.row354;

        rowIds[i++] = R.id.row355;

        rowIds[i++] = R.id.row356;

        rowIds[i++] = R.id.row357;

        rowIds[i++] = R.id.row358;

        rowIds[i++] = R.id.row359;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row360;

        rowIds[i++] = R.id.row361;

        rowIds[i++] = R.id.row362;

        rowIds[i++] = R.id.row363;

        rowIds[i++] = R.id.row364;

        rowIds[i++] = R.id.row365;

        rowIds[i++] = R.id.row366;

        rowIds[i++] = R.id.row367;

        rowIds[i++] = R.id.row368;

        rowIds[i++] = R.id.row369;

        rowIds[i++] = R.id.row370;

        rowIds[i++] = R.id.row371;

        rowIds[i++] = R.id.row372;

        rowIds[i++] = R.id.row373;

        rowIds[i++] = R.id.row374;

        rowIds[i++] = R.id.row375;

        rowIds[i++] = R.id.row376;

        rowIds[i++] = R.id.row377;

        rowIds[i++] = R.id.row378;

        rowIds[i++] = R.id.row379;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row380;

        rowIds[i++] = R.id.row381;

        rowIds[i++] = R.id.row382;

        rowIds[i++] = R.id.row383;

        rowIds[i++] = R.id.row384;

        rowIds[i++] = R.id.row385;

        rowIds[i++] = R.id.row386;

        rowIds[i++] = R.id.row387;

        rowIds[i++] = R.id.row388;

        rowIds[i++] = R.id.row389;

        rowIds[i++] = R.id.row390;

        rowIds[i++] = R.id.row391;

        rowIds[i++] = R.id.row392;

        rowIds[i++] = R.id.row393;

        rowIds[i++] = R.id.row394;

        rowIds[i++] = R.id.row395;

        rowIds[i++] = R.id.row396;

        rowIds[i++] = R.id.row397;

        rowIds[i++] = R.id.row398;

        rowIds[i++] = R.id.row399;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row400;

        rowIds[i++] = R.id.row401;

        rowIds[i++] = R.id.row402;

        rowIds[i++] = R.id.row403;

        rowIds[i++] = R.id.row404;

        rowIds[i++] = R.id.row405;

        rowIds[i++] = R.id.row406;

        rowIds[i++] = R.id.row407;

        rowIds[i++] = R.id.row408;

        rowIds[i++] = R.id.row409;

        rowIds[i++] = R.id.row410;

        rowIds[i++] = R.id.row411;

        rowIds[i++] = R.id.row412;

        rowIds[i++] = R.id.row413;

        rowIds[i++] = R.id.row414;

        rowIds[i++] = R.id.row415;

        rowIds[i++] = R.id.row416;

        rowIds[i++] = R.id.row417;

        rowIds[i++] = R.id.row418;

        rowIds[i++] = R.id.row419;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row420;

        rowIds[i++] = R.id.row421;

        rowIds[i++] = R.id.row422;

        rowIds[i++] = R.id.row423;

        rowIds[i++] = R.id.row424;

        rowIds[i++] = R.id.row425;

        rowIds[i++] = R.id.row426;

        rowIds[i++] = R.id.row427;

        rowIds[i++] = R.id.row428;

        rowIds[i++] = R.id.row429;

        rowIds[i++] = R.id.row430;

        rowIds[i++] = R.id.row431;

        rowIds[i++] = R.id.row432;

        rowIds[i++] = R.id.row433;

        rowIds[i++] = R.id.row434;

        rowIds[i++] = R.id.row435;

        rowIds[i++] = R.id.row436;

        rowIds[i++] = R.id.row437;

        rowIds[i++] = R.id.row438;

        rowIds[i++] = R.id.row439;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row440;

        rowIds[i++] = R.id.row441;

        rowIds[i++] = R.id.row442;

        rowIds[i++] = R.id.row443;

        rowIds[i++] = R.id.row444;

        rowIds[i++] = R.id.row445;

        rowIds[i++] = R.id.row446;

        rowIds[i++] = R.id.row447;

        rowIds[i++] = R.id.row448;

        rowIds[i++] = R.id.row449;

        rowIds[i++] = R.id.row450;

        rowIds[i++] = R.id.row451;

        rowIds[i++] = R.id.row452;

        rowIds[i++] = R.id.row453;

        rowIds[i++] = R.id.row454;

        rowIds[i++] = R.id.row455;

        rowIds[i++] = R.id.row456;

        rowIds[i++] = R.id.row457;

        rowIds[i++] = R.id.row458;

        rowIds[i++] = R.id.row459;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row460;

        rowIds[i++] = R.id.row461;

        rowIds[i++] = R.id.row462;

        rowIds[i++] = R.id.row463;

        rowIds[i++] = R.id.row464;

        rowIds[i++] = R.id.row465;

        rowIds[i++] = R.id.row466;

        rowIds[i++] = R.id.row467;

        rowIds[i++] = R.id.row468;

        rowIds[i++] = R.id.row469;

        rowIds[i++] = R.id.row470;

        rowIds[i++] = R.id.row471;

        rowIds[i++] = R.id.row472;

        rowIds[i++] = R.id.row473;

        rowIds[i++] = R.id.row474;

        rowIds[i++] = R.id.row475;

        rowIds[i++] = R.id.row476;

        rowIds[i++] = R.id.row477;

        rowIds[i++] = R.id.row478;

        rowIds[i++] = R.id.row479;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row480;

        rowIds[i++] = R.id.row481;

        rowIds[i++] = R.id.row482;

        rowIds[i++] = R.id.row483;

        rowIds[i++] = R.id.row484;

        rowIds[i++] = R.id.row485;

        rowIds[i++] = R.id.row486;

        rowIds[i++] = R.id.row487;

        rowIds[i++] = R.id.row488;

        rowIds[i++] = R.id.row489;

        rowIds[i++] = R.id.row490;

        rowIds[i++] = R.id.row491;

        rowIds[i++] = R.id.row492;

        rowIds[i++] = R.id.row493;

        rowIds[i++] = R.id.row494;

        rowIds[i++] = R.id.row495;

        rowIds[i++] = R.id.row496;

        rowIds[i++] = R.id.row497;

        rowIds[i++] = R.id.row498;

        rowIds[i++] = R.id.row499;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row500;

        rowIds[i++] = R.id.row501;

        rowIds[i++] = R.id.row502;

        rowIds[i++] = R.id.row503;

        rowIds[i++] = R.id.row504;

        rowIds[i++] = R.id.row505;

        rowIds[i++] = R.id.row506;

        rowIds[i++] = R.id.row507;

        rowIds[i++] = R.id.row508;

        rowIds[i++] = R.id.row509;

        rowIds[i++] = R.id.row510;

        rowIds[i++] = R.id.row511;

        rowIds[i++] = R.id.row512;

        rowIds[i++] = R.id.row513;

        rowIds[i++] = R.id.row514;

        rowIds[i++] = R.id.row515;

        rowIds[i++] = R.id.row516;

        rowIds[i++] = R.id.row517;

        rowIds[i++] = R.id.row518;

        rowIds[i++] = R.id.row519;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row520;

        rowIds[i++] = R.id.row521;

        rowIds[i++] = R.id.row522;

        rowIds[i++] = R.id.row523;

        rowIds[i++] = R.id.row524;

        rowIds[i++] = R.id.row525;

        rowIds[i++] = R.id.row526;

        rowIds[i++] = R.id.row527;

        rowIds[i++] = R.id.row528;

        rowIds[i++] = R.id.row529;

        rowIds[i++] = R.id.row530;

        rowIds[i++] = R.id.row531;

        rowIds[i++] = R.id.row532;

        rowIds[i++] = R.id.row533;

        rowIds[i++] = R.id.row534;

        rowIds[i++] = R.id.row535;

        rowIds[i++] = R.id.row536;

        rowIds[i++] = R.id.row537;

        rowIds[i++] = R.id.row538;

        rowIds[i++] = R.id.row539;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row540;

        rowIds[i++] = R.id.row541;

        rowIds[i++] = R.id.row542;

        rowIds[i++] = R.id.row543;

        rowIds[i++] = R.id.row544;

        rowIds[i++] = R.id.row545;

        rowIds[i++] = R.id.row546;

        rowIds[i++] = R.id.row547;

        rowIds[i++] = R.id.row548;

        rowIds[i++] = R.id.row549;

        rowIds[i++] = R.id.row550;

        rowIds[i++] = R.id.row551;

        rowIds[i++] = R.id.row552;

        rowIds[i++] = R.id.row553;

        rowIds[i++] = R.id.row554;

        rowIds[i++] = R.id.row555;

        rowIds[i++] = R.id.row556;

        rowIds[i++] = R.id.row557;

        rowIds[i++] = R.id.row558;

        rowIds[i++] = R.id.row559;

        if(numberOfRows < i)
            return rowIds;

        rowIds[i++] = R.id.row560;

        rowIds[i++] = R.id.row561;

        rowIds[i++] = R.id.row562;

        rowIds[i++] = R.id.row563;

        rowIds[i++] = R.id.row564;

        rowIds[i++] = R.id.row565;

        rowIds[i++] = R.id.row566;

        rowIds[i++] = R.id.row567;

        rowIds[i++] = R.id.row568;

        rowIds[i++] = R.id.row569;

        rowIds[i++] = R.id.row570;

        rowIds[i++] = R.id.row571;

        rowIds[i++] = R.id.row572;

        rowIds[i++] = R.id.row573;

        rowIds[i++] = R.id.row574;

        rowIds[i++] = R.id.row575;

        rowIds[i++] = R.id.row576;

        rowIds[i++] = R.id.row577;

        rowIds[i++] = R.id.row578;

        rowIds[i++] = R.id.row579;



        return rowIds;

    }
}
